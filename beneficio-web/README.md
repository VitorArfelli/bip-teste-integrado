# Beneficio Web

Frontend Angular do BenefitOps para gestão de benefícios e transferências.

## Comandos

```bash
npm install
npm start
npm test -- --watch=false --browsers=ChromeHeadless
npm run build
```

O `npm start` usa `proxy.conf.json` para encaminhar chamadas `/api` para `http://localhost:8080`.
